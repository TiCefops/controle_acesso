import 'package:flutter/material.dart';
class CadastroHospitalWidget extends StatelessWidget {
  const CadastroHospitalWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('DDDDDDDDD'),
    );
  }
}
