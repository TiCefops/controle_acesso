import 'package:flutter/material.dart';

class ListarDisciplinasWidget extends StatelessWidget {
  const ListarDisciplinasWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
